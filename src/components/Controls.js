import Button from "./Button";

const Controls = () => {
  return (
    <div>
      <Button name="Start" />
      <Button name="Pause" />
      <Button name="Reset" />
    </div>
  );
};

export default Controls;
